export enum InputType {
  TEXT = 'text',
  PASSWORD = 'password',
  NUMBER = 'number',
}

export enum TextTransform {
  UPPERCASE = 'uppercase',
  CAPITALIZE = 'capitalize',
}

export enum Localization {
  TR = 'tr',
  EN = 'en',
}
